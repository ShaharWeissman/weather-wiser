or the icon and increased the size of the temperature text
return (
  <div className="current-weather">
    <div className="weather-header">Current Weather</div>
    <div className="weather-icon-temp">
      <div className="icon-container"> {/* Container for the icon */}
        <img src={weatherIconUrl} alt="Weather icon" className="icon-weather" />
      </div>
      <div className="temperature-container"> {/* Container for the temperature */}
        <p className="temperature-text">
          {locationDetails?.Temperature?.[isMetric ? "Metric" : "Imperial"]?.Value}°{isMetric ? "C" : "F"}
        </p>
      </div>
    </div>
    <div className="favorites">
      {/* Favorites button */}
    </div>
    <div className="description">
      <h2>{locationDetails?.WeatherText}</h2>
    </div>
  </div>
);
CSS:

css
Copy code
.weather-header {
  /* ... */
}

.icon-container {
  border: 3px solid #507dac; /* Added border */
  padding: 10px; /* Padding around the icon */
  border-radius: 50%; /* Rounded border */
  background-color: #f0f0f0; /* Background color for the icon */
  display: inline-block; /* Changed to inline-block for better control */
}

.icon-weather {
  width: 100px; /* Increased width for a larger icon */
  height: auto; /* Maintain aspect ratio */
}

.temperature-container {
  /* Container for temperature to position it next to the icon */
  display: flex;
  align-items: center; /* Vertically center the temperature text */
}

.temperature-text {
  font-size: 2em; /* Enlarged temperature text */
  font-weight: bold; /* Made temperature text bold */
  color: #072b41; /* Styled temperature text color */
}

/* ... other styles ... */